---
title: "HW04"
author: "Nick Begany"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
con = DBI::dbConnect(RSQLite::SQLite(), "C:/Users/nbb17/OneDrive/Documents/business_intelligence/data/midwest_airbnb.db")
listings = DBI::dbReadTable(con, "listings")

nrow(listings)
```

```{r}
sum(listings$city == "Columbus")
```
```{r}
listings[listings$city == "Chicago" & 
     listings$price == max(listings$price[listings$city == "Chicago"]), "name"]
```

```{r}
length(unique(listings$neighbourhood[listings$city == "Chicago"]))
```
```{r}
sum(listings$host_is_superhost == "t", na.rm = TRUE)
```

```{r}
sum(listings$price > 1000, na.rm = TRUE)
```

